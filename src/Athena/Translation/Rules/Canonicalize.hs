
-- | Athena.Translation.Rule.Canonicalize module.

{-# LANGUAGE ScopedTypeVariables #-}
{-# LANGUAGE UnicodeSyntax       #-}

module Athena.Translation.Rules.Canonicalize ( atpCanonicalize ) where

------------------------------------------------------------------------------

import Data.TSTP ( Formula(..) )

------------------------------------------------------------------------------

atpCanonicalize ∷ Formula → Formula → String
atpCanonicalize = undefined
